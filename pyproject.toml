[build-system]
requires = [
    "numpy",
    "meson-python",
]
build-backend = "mesonpy"

[project]
name = "py_euler_ale"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26.4",
    "scipy>=1.15.3",
]
